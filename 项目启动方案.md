# AICIA 项目启动方案

> 本文件是前期调研与技术选型的汇总，作为新项目开发的起点。
> 整理日期：2026-09-19

---

## 一、项目定位

复刻并改进 **Token FBI**（https://github.com/hope0719/token-fbi ）——
一个"免费 AI Token 情报站"：汇总各家大模型/AI 工具的免费额度、领取方式和实测评价，以卡片墙形式展示。

- 参考站线上地址：https://token-fbi.com/
- 技术形态：纯静态网站，无后端、无数据库
- 托管方式：serverless 静态托管（Cloudflare Pages 为主选）
- 维护方式：可视化后台（Sveltia CMS），编辑者填表即可更新内容，无需懂代码

---

## 二、参考项目（token-fbi）技术摸底结论

### 2.1 它现在是什么

- 三个核心文件：`index.html`（12KB）+ `style.css`（29KB）+ `app.js`（49KB）
- `app.js` 里硬编码了约 30 条卡片数据（`TOKENS` 数组）+ 渲染逻辑 + 分类筛选
- 构建脚本 `scripts/build-site.mjs`：用 Node VM 沙箱跑 app.js 前半段，把卡片**预渲染成静态 HTML** 注入模板，用于 SEO
- 构建产物输出到 `dist/`，额外生成 `robots.txt` / `sitemap.xml` / `llms.txt` / IndexNow 验证文件
- 零运行时后端请求（已 grep 确认无 fetch/XHR/axios）
- `.env` 为空文件，无环境变量依赖

### 2.2 它现在怎么部署

| 平台 | 用途 | 配置 |
|---|---|---|
| Cloudflare Pages | 主部署 | 连接 GitHub main，构建命令 `npm run build`，输出目录 `dist`，绑自定义域名 token-fbi.com |
| GitHub Pages | 备用 | 从根目录直接发布，不跑构建 |
| EdgeOne Pages | 适配备选 | 仓库里已有 `.edgeone/` 配置 |
| GitHub Actions | IndexNow 推送 | 构建完成后自动 ping 搜索引擎 |

### 2.3 继承时要避开的坑（代码层面已发现的问题）

复刻时不要照抄，以下问题需要在新项目里修正：

1. **XSS 转义不一致（严重）**：
   - 构建脚本（Node 端）用了 `escapeHtml()` 做转义
   - 但浏览器端 `render()` 函数只用 `cleanText()`（把 `—–` 替换成 `-`），**没有 HTML 转义**
   - 卡片数据里若出现特殊字符，运行时重新渲染会出问题；未来 CMS 录入内容更要注意

2. **海报弹窗事件丢失（bug）**：
   - `initPosterModal()` 只在初始化时绑定一次事件
   - 用户点筛选按钮触发 `render()` 重建 `innerHTML` 后，海报按钮的事件监听丢失
   - 新项目要用**事件委托**或重新绑定

3. **缓存策略错误**：
   - index.html 头部写了 `Cache-Control: no-cache, no-store, must-revalidate`
   - 这会让浏览器和 CDN 每次都拉完整 HTML，完全放弃缓存
   - 静态站应该用文件指纹长缓存（现在已有 `?v=yyyymmddxx` 但 HTML 本身被 no-store 了）

4. **GSAP 依赖过重**：
   - 整个站只用 GSAP 做卡片淡入动画，从 CDN 拉了 ~60KB
   - 新项目用 CSS `@keyframes` + `IntersectionObserver` 即可，零依赖

5. **数据与逻辑耦合**：
   - 30 条卡片数据和 500 行渲染逻辑塞在一个 49KB 文件里
   - 观望名单的 16 个平台同时在 `TOKENS` 和 `DONOTS` 里重复定义
   - 精选门槛正则、index.html 列表、README 三处需手动同步（已发生过不同步事故）

6. **构建脚本脆弱**：
   - 靠 `indexOf('function render(type) {')` 字符串切分 app.js
   - 函数名改了就崩
   - 硬编码 `siteUrl = 'https://token-fbi.com/'`，复刻时必须改

---

## 三、新项目技术选型

### 3.1 托管平台

**主选：Cloudflare Pages**
- 免费额度无限带宽、无限请求
- 支持 Pages Functions（跑 OAuth 代理）
- Git 集成自动构建
- 国内访问偶尔抽风，但可绑自定义域名缓解

**备选：EdgeOne Pages（腾讯）**——国内访问最快，仓库模板已验证可行

### 3.2 CMS 选型：Sveltia CMS（已定）

在 Decap CMS vs TinaCMS 中选择了 **Sveltia CMS**，原因：

| 维度 | Sveltia CMS | TinaCMS |
|---|---|---|
| 后台页面 | 托管在自己域名（CDN 可控） | 编辑器核心从 tina.io 拉取 |
| 认证 | Pages Function 自建 OAuth，同域 | 依赖 Tina Cloud（tina.io） |
| 免费额度 | 完全免费、无人数限制 | 免费版仅 2 用户 |
| 对纯静态 HTML | 加一个 admin/ 目录即可 | 要求迁移到 Next.js/Astro |
| 国内网络 | 不依赖境外 SaaS（除 GitHub 登录） | 强依赖 tina.io，单点风险 |

Sveltia 是 Decap CMS 的 drop-in 替换，配置格式完全兼容，CDN 加载：
`https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js`

---

## 四、目标架构

```
浏览器
  │
  ├── 你的域名/            → 静态卡片站（Cloudflare Pages 构建产物）
  │
  └── 你的域名/admin/      → Sveltia CMS SPA（纯前端）
         │
         ├── 登录：你的域名/api/callback → Pages Function OAuth 代理 → GitHub
         │
         └── 编辑保存：Sveltia CMS → GitHub API 直接 commit 到 main
                                      │
                                      └── 触发 Cloudflare Pages 自动构建
                                            │
                                            └── 构建时：读 data/cards/*.yml
                                                      → 生成 app.js + 预渲染 HTML
                                                      → 上线
```

**关键原则**：所有编辑操作最终都变成 GitHub commit，部署完全由 Git 驱动。

---

## 五、目标目录结构

```
AICIA/
├── data/cards/                  # 每条卡片一个 YAML 文件（CMS 编辑对象）
│   ├── platform-a.yml
│   ├── platform-b.yml
│   └── ...
├── data/watchout.yml            # 观望名单（独立配置）
├── admin/
│   ├── index.html               # Sveltia CMS 入口（引用 CDN 脚本）
│   └── config.yml               # CMS collections / fields 映射
├── functions/
│   └── api/[[path]].js          # OAuth 代理（Cloudflare Pages Functions）
├── scripts/
│   └── build-site.mjs           # 改造：读 YAML → 生成 app.js + 预渲染 HTML
├── public/
│   ├── index.html               # 页面骨架（含 STATIC_CARDS 占位符）
│   ├── style.css
│   └── images/                  # 二维码 / 海报等
├── package.json                 # 依赖：js-yaml（构建时用）
└── README.md
```

---

## 六、部署步骤（按动手顺序）

### 第 1 步：数据拆分（本地先跑通）
- 把 token-fbi `app.js` 里的 `TOKENS` 数组逐条拆成 `data/cards/*.yml`
- 重写 `scripts/build-site.mjs`：遍历 YAML → 生成 `app.js` → 注入 `index.html` 占位符 → 输出 `dist/`
- 本地 `npm run build` 验证产物和原站一致

### 第 2 步：推上 Cloudflare Pages 验证静态站
- 连接 GitHub 仓库
- 构建命令 `npm run build`，输出目录 `dist`
- 先不加 CMS，确认站点本身正常

### 第 3 步：加 admin/ 后台
- 写 `admin/index.html`（引用 Sveltia CDN）
- 写 `admin/config.yml`（把 YAML 字段映射成表单）
- 暂不配置 backend，先确认页面能打开

### 第 4 步：GitHub OAuth App
- GitHub → Settings → Developer settings → OAuth Apps → New
- Homepage URL：你的正式域名
- Callback URL：`https://你的域名/api/callback`
- 记下 Client ID / Client Secret

### 第 5 步：部署 OAuth 代理
- 方案 A（推荐）：用官方 `sveltia-cms-auth` Worker 模板一键部署到 Cloudflare Workers
- 方案 B：在仓库里写 `functions/api/[[path]].js` 实现 OAuth 交换
- 配置环境变量：`GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` / `DEPLOY_DOMAIN`
- 把 Worker URL 填回 `admin/config.yml` 的 `backend.base_url`

### 第 6 步：端到端测试
- 打开 `/admin/` → GitHub 登录 → 看到所有 YAML 文件
- 改一条卡片的 `updated` 日期 → Publish → 等 Pages 重建 → 线上生效

---

## 七、待办事项

- [ ] 确定项目正式名称和品牌（AICIA 暂定）
- [ ] 确定自定义域名（先用 `*.pages.dev` 也可）
- [ ] 设计新项目的视觉风格（原站是深色"情报局风 + 琥珀金"，是否沿用？）
- [ ] 第 1 步：数据拆分脚本 + YAML 数据文件
- [ ] 第 2 步：Cloudflare Pages 项目创建
- [ ] 第 3-6 步：CMS + OAuth 链路打通
- [ ] 修复原站继承的 6 个代码问题（见 2.3 节）

---

## 八、关键参考链接

- 原项目：https://github.com/hope0719/token-fbi
- Sveltia CMS 官方文档：https://sveltiacms.app/en/docs/start
- Sveltia CMS OAuth Worker：https://github.com/sveltia/sveltia-cms-auth
- Cloudflare Pages OAuth 实践：https://sheng.page/en/posts/adding-decap-cms-to-hugo-on-cloudflare-pages/
- Cloudflare Pages 控制台：https://dash.cloudflare.com/
