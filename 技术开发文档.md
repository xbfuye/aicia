# AICIA 技术开发文档

> 版本：v1.0 · 更新日期：2026-09-19
> 本文档是项目从选型到落地的完整技术方案，开发时以此为准。

---

## 一、项目概述

**AI CIA**（项目代号 AICIA）是一个"免费 AI Token 情报站"，汇总各家大模型与 AI 工具的免费额度、支持模型、领取方式和实测评价，以卡片墙形式公开展示。

- 正式名称：**AI CIA**
- 默认域名：https://aicia.pages.dev（后续可绑自有域名）
- 参考项目：https://github.com/hope0719/token-fbi （线上 https://token-fbi.com/）
- 内容形态：卡片墙（大模型类 / 工具类 / 项目类）+ **观望名单（保留）**
- 维护模式：站长与协作者通过可视化后台录入，普通访客通过表单投稿，站长审核后上线
- 成本目标：域名 + Cloudflare 免费额度，月运营成本接近 0

---

## 二、核心设计原则

1. **Git 即数据库**：所有内容（卡片、观望名单、投稿）都是 GitHub 仓库里的 YAML 文件，不用独立数据库
2. **薄后端**：服务端只做三件事——收表单、调 GitHub API、做 OAuth 代理，没有业务逻辑
3. **平台无关**：核心代码不绑定任何 serverless 厂商，未来可迁 Vercel / EdgeOne / 自建 PHP
4. **静态优先**：访客看到的最终页面是构建产物（预渲染 HTML），不需要 JS 执行就能看到内容（SEO 友好）
5. **权限分层**：访客匿名投稿，管理员 GitHub 登录审核，二者身份隔离

---

## 三、技术选型

### 3.1 托管平台

| 项目 | 选择 | 理由 |
|---|---|---|
| 主托管 | **Cloudflare Pages** | 免费、无限带宽、自带 Pages Functions、与 Sveltia CMS 生态最顺 |
| 备用 | Vercel / EdgeOne Pages | 架构已解耦，迁移时只改函数入口文件 |
| 不选 | GitHub Pages | 不支持服务端函数，无法独立做投稿表单与 OAuth |

### 3.2 内容管理后台

**选择 Sveltia CMS**，在 Decap CMS 与 TinaCMS 之间对比后的结论：

| 维度 | Sveltia CMS | TinaCMS |
|---|---|---|
| 后台页面 | 托管在自己域名 | 编辑器核心从 tina.io 拉取 |
| 认证 | 自建 OAuth，同域 | 依赖 Tina Cloud（tina.io） |
| 免费额度 | 完全免费、无人数限制 | 免费版仅 2 用户 |
| 对纯静态 HTML | 加一个 admin/ 目录即可 | 要求 Next.js/Astro 重写 |
| 国内网络 | 不依赖境外 SaaS | 强依赖 tina.io，单点风险 |

Sveltia 是 Decap CMS 的 drop-in 替换，配置格式兼容，CDN 引入：
`https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js`

### 3.3 技术栈

- 前端：原生 HTML + CSS + Vanilla JS（不引框架，不引 GSAP，动画用 CSS + IntersectionObserver）
- 构建：Node.js 脚本（`scripts/build-site.mjs`）
- 后端：Cloudflare Pages Functions（原生 JS，无框架）
- 数据格式：YAML（`data/cards/*.yml`）
- 反垃圾：Cloudflare Turnstile
- 通知：飞书自定义机器人 webhook

### 3.4 视觉设计方向：去 AI 味

不复用 token-fbi 的"深色情报局风 + 琥珀金"。目标是做出一份**克制、像传统编辑刊物**的情报小报，而不是又一个 AI SaaS 官网。

**要避免的"AI 味"特征**：
- 紫蓝渐变、霓虹发光、玻璃拟态大圆角卡片
- 满屏 emoji 装饰、icon 满天飞
- "一站式 / 全栈 / 赋能 / 智能"这类 SaaS 话术
- 模板感强烈的 hero 区（大标题 + 三个按钮 + 假数据指标）
- 同质化的"科技蓝"配色

**要走的方向**：
- **排版为主，装饰为辅**：用字体层级（衬线标题 + 等宽数据）建立视觉，不靠色块和渐变
- **克制配色**：米白纸感底色（#faf8f4 类似）+ 墨黑文字 + 单一克制强调色（可选，不要多色）
- **情报小报质感**：像《The Information》《晚点 LatePost》那种编辑设计，条目清晰、日期明确、像一份手写内参
- **数据真实感**：每条卡片显示具体数字（100 万 token、30 RPM、截止日期），不写"海量""不限量"这种虚词
- **零过度动效**：不要入场动画漫天飞，只在必要处用 1-2 个克制过渡
- **字体建议**：标题用思源宋体 / Noto Serif SC 或系统衬线；数据/代码用 JetBrains Mono / 系统等宽；正文用系统无衬线

> 设计原则：让用户第一眼觉得"这是一份认真做的内参"，而不是"又一个套模板的 AI 工具站"。

---

## 四、系统架构

### 4.1 三个角色，两套身份

| 角色 | 操作方式 | 是否登录 | 用什么身份 |
|---|---|---|---|
| 访客 | 填表投稿 | 否，匿名 | 无身份，直接 POST |
| Pages Function | 收到表单，写仓库文件 | 对访客透明 | 站长预存的 `GITHUB_PAT`（仓库写权限） |
| 管理员 | 审核、补字段、发布 | 是 | 自己的 GitHub 账号 OAuth |

### 4.2 完整数据流

```
【访客投稿链路】
访客打开 /contribute.html
  → 填表（Turnstile 验证）
  → POST /api/submit
  → Pages Function:
      ① 验 Turnstile token
      ② 用 GITHUB_PAT 调 GitHub Contents API
         在 data/cards/ 下新建 yml 文件（status: pending）
      ③ POST 飞书机器人 webhook 通知站长
  → 返回成功页

【管理员审核链路】
管理员打开 /admin/
  → GitHub OAuth 登录（走 /api/callback 代理）
  → Sveltia CMS 展示"待审核"视图（filter status=pending）
  → 点开投稿，补 rating / 精选判断 / tone
  → 改 status 为 approved
  → Save（commit 到 main）

【上线链路】
main 收到新 commit
  → Cloudflare Pages 自动构建
  → build-site.mjs:
      ① 读 data/cards/*.yml
      ② 只渲染 status=approved 的卡片
      ③ 注入 index.html 占位符
      ④ 输出 dist/（含 robots.txt / sitemap.xml / llms.txt）
  → 静态文件上线
```

### 4.3 架构图

```
                    浏览器
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
   / (卡片站)    /admin/ (CMS)   /contribute.html
   静态 HTML     Sveltia SPA     投稿表单
        │             │             │
        │             ↓             ↓
        │        /api/callback   /api/submit
        │        OAuth 代理       Pages Function
        │             │             │
        │             └──────┬──────┘
        │                    ↓
        │              api.github.com
        │              (读写仓库 YAML)
        ↓
   Cloudflare CDN 静态分发
```

---

## 五、数据模型

### 5.1 卡片字段（data/cards/*.yml）

```yaml
# 平台基本信息
name: "WorkBuddy"                # 必填，平台名
type: "工具"                      # 必填：大模型 / 工具 / 项目
modality: "HY3 · DeepSeek-V4.1"  # 必填，支持的模型

# 评价（管理员补）
rating: 5                        # 必填，1-5 星
tone: "green"                    # 可选：yellow / green / orange / null
badge: "九月限定"                # 可选，右上角角标

# 内容
quota: "免费额度说明..."          # 必填
signup: "注册奖励说明..."         # 可选
effect: "实际体验..."             # 必填
link: "https://..."               # 必填，领取链接

# 状态（投稿时自动填，管理员改）
status: "pending"                 # pending / approved / rejected
updated: "2026-09-19"             # 必填，最近更新日期
limited: "2026-10-10"             # 可选，限时截止日

# 展示控制（管理员配置）
alwaysShow: false                 # 可选，广告卡强制展示
pin: 4                            # 可选，固定位次
```

### 5.2 观望名单（data/watchout.yml）

```yaml
watchout:
  - name: "某平台"
    why: "额度太小 / 不稳定"
    link: "https://..."
```

### 5.3 精选门槛规则（scripts/build-site.mjs 内）

与首页"精选门槛"列表保持单一数据源：

```js
const FEATURED_RULES = [
  { label: "DeepSeek V4",  re: /deepseekv(?:[4-9]|1\d)/ },
  { label: "GLM 5.2",      re: /glm5(?:[2-9]|1\d)|glm[6-9]/ },
  { label: "Kimi K3",      re: /kimik(?:[3-9]|1\d)/ },
  { label: "千问 3.8 Max",  re: /(?:千问|qwen)3(?:[8-9]|1\d)|(?:千问|qwen)[4-9]/ },
  { label: "Hy3",          re: /hy(?:[3-9]|1\d)/ },
  { label: "LongCat 2.0",  re: /longcat/ }
];
```

> 注意：原项目此处与首页 HTML、README 三处手动同步，曾出现不一致。新项目里把门槛规则做成单一数据源，HTML 列表由构建脚本生成。

---

## 六、目录结构

```
AICIA/
├── data/
│   ├── cards/                       # 卡片数据（投稿与维护都在这里）
│   │   ├── worksbuddy.yml
│   │   └── ...
│   └── watchout.yml                 # 观望名单
│
├── admin/                           # Sveltia CMS 后台
│   ├── index.html                   # 引用 CDN 脚本
│   └── config.yml                   # collections 与 fields 映射
│
├── functions/                       # Cloudflare Pages Functions
│   └── api/
│       ├── [[path]].js              # OAuth 代理（GitHub callback）
│       └── submit.js                # 投稿接收端点
│
├── public/                          # 前端源文件（构建前）
│   ├── index.html                   # 骨架，含 <!-- STATIC_CARDS --> 占位
│   ├── contribute.html              # 投稿表单页
│   ├── style.css
│   ├── app.js                       # 浏览器交互逻辑（筛选、动画）
│   └── images/
│       ├── qrcode-wechat.jpg
│       └── poster-*.jpg
│
├── scripts/
│   └── build-site.mjs               # 构建：读 YAML → 生成 dist/
│
├── package.json
├── wrangler.toml                    # Cloudflare Pages 配置
└── README.md
```

---

## 七、核心组件设计

### 7.1 Sveltia CMS 配置（admin/config.yml）

```yaml
backend:
  name: github
  repo: 你的用户名/仓库名
  branch: main
  base_url: https://oauth.你的域名.workers.dev   # OAuth Worker 地址

media_folder: "public/images"
public_folder: "/images"

collections:
  # 待审核视图
  - name: pending
    label: 待审核投稿
    folder: data/cards
    create: false
    delete: true
    extension: yml
    format: yml
    filter: { field: status, value: pending }
    fields: [/* 同卡片字段 */]

  # 已上线视图
  - name: approved
    label: 已上线卡片
    folder: data/cards
    create: true
    delete: true
    extension: yml
    format: yml
    filter: { field: status, value: approved }
    fields: [/* 同卡片字段 */]
```

> 两个 collection 共用同一套 fields 定义，只是 filter 不同。管理员在"待审核"里补完字段后把 status 改成 approved，卡片自动出现在"已上线"里并被构建渲染。

### 7.2 投稿接收端点（functions/api/submit.js）

职责：
1. 从 POST body 读取表单字段
2. 调 Cloudflare Turnstile API 验证真人
3. 用 `GITHUB_PAT` 调 GitHub Contents API，在 `data/cards/` 下新建文件
   - 文件名：`平台名-时间戳.yml`，防冲突
   - 默认值：`status: pending`、`rating: 3`、`updated: 今天`
4. POST 飞书机器人 webhook，消息体：新投稿平台名 + 链接 + 摘要
5. 返回 JSON 给前端

环境变量（在 Cloudflare Pages 控制台配置）：
- `GITHUB_PAT`：仓库 Contents 读写权限
- `GITHUB_REPO`：`owner/repo`
- `TURNSTILE_SECRET_KEY`：Turnstile 密钥
- `FEISHU_WEBHOOK_URL`：飞书群机器人 webhook

### 7.3 投稿表单页（public/contribute.html）

表单字段与卡片 YAML 字段**一一对应**，访客填完后，管理员在 CMS 里看到的就是已经填好这些字段的 yml，只需要补专业判断项。

| 表单字段 | 控件 | 必填 | 对应 YAML 字段 |
|---|---|---|---|
| 平台名称 | 单行文本 | 是 | `name` |
| 官网/领取链接 | URL | 是 | `link` |
| 平台类型 | 单选：大模型 / 工具 / 项目 | 是 | `type` |
| 支持哪些模型 | 单行文本 | 是 | `modality` |
| 免费额度说明 | 多行文本 | 是 | `quota` |
| 实际体验/推荐理由 | 多行文本 | 是 | `effect` |
| 是否限时 | 单选 + 日期 | 否 | `limited` |
| 你的联系方式（选填） | 单行文本 | 否 | `contact`（仅管理员可见，不渲染到卡片） |

**管理员在 CMS 里额外补的字段**（访客填不了，属于专业判断）：
- `rating`（1-5 星）
- `tone`（卡片色调）
- `badge`（角标，可选）
- `alwaysShow` / `pin`（展示控制）
- `status`（从 pending 改为 approved）

其他设计要求：
- 内嵌 Cloudflare Turnstile 小组件
- 提交用 `fetch()` POST 到 `/api/submit`
- 成功后显示"已收到，3 天内核实，采用后将上线"
- 不需要任何账号、不跳任何第三方
- 表单视觉风格与主站一致（编辑刊物风，不要 SaaS 风）

### 7.4 OAuth 代理（functions/api/[[path]].js）

- Sveltia CMS 登录时，GitHub 把 authorization code 发到这里
- 用 `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` 换成 access token
- 回调 URL：`https://你的域名/api/callback`
- 这部分逻辑可以直接复用官方 sveltia-cms-auth Worker 模板

### 7.5 构建脚本（scripts/build-site.mjs）

职责：
1. 读 `data/cards/*.yml`，`js-yaml` 解析
2. 过滤 `status === 'approved'`
3. 应用精选规则（isFeatured）与观望名单排除
4. 排序（pin 字段优先）
5. 把卡片预渲染成 HTML，注入 `index.html` 的 `<!-- STATIC_CARDS -->` 占位符
6. 生成 JSON-LD 结构化数据、robots.txt、sitemap.xml、llms.txt
7. 输出到 `dist/`

**与原项目的区别**：
- 不再用 VM 沙箱跑 app.js 切字符串，直接读 YAML，干净可靠
- 不再硬编码 siteUrl，从环境变量或配置文件读
- 构建失败时报字段缺失错误（缺 name/link/quota 直接构建报错）

---

## 八、部署步骤

### 阶段 1：本地数据层（不碰线上）

- [ ] 把 token-fbi 的 TOKENS 数组拆成 `data/cards/*.yml`
- [ ] 写新的 `scripts/build-site.mjs`，本地 `npm run build` 验证 dist/ 产物
- [ ] 本地起静态服务确认页面正常

### 阶段 2：Cloudflare Pages 首次部署

- [ ] 新建 GitHub 仓库，push 代码
- [ ] Cloudflare Pages 连接仓库
- [ ] 构建命令：`npm run build`，输出目录：`dist`
- [ ] 配置环境变量（先不配 OAuth，先把静态站跑起来）
- [ ] 绑自定义域名（可选，先用 `*.pages.dev`）

### 阶段 3：CMS 后台

- [ ] 写 `admin/index.html` 和 `admin/config.yml`
- [ ] 建 GitHub OAuth App（callback: `https://域名/api/callback`）
- [ ] 部署 OAuth 代理（用 sveltia-cms-auth 模板）
- [ ] 填 `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` / `DEPLOY_DOMAIN`
- [ ] 验证：打开 `/admin/` 能登录、能看到 YAML 文件、能编辑保存

### 阶段 4：投稿链路

- [ ] 建 Cloudflare Turnstile site
- [ ] 建飞书群自定义机器人，拿 webhook URL
- [ ] 写 `public/contribute.html` 表单
- [ ] 写 `functions/api/submit.js`
- [ ] 配置环境变量：`GITHUB_PAT` / `TURNSTILE_SECRET_KEY` / `FEISHU_WEBHOOK_URL`
- [ ] 端到端测试：匿名提交 → 仓库出现 yml → 飞书收到通知 → CMS 能看到待审核

### 阶段 5：打磨

- [ ] 修复继承自 token-fbi 的 6 个坑（见第九节）
- [ ] 加 favicon、og:image
- [ ] 加投稿成功页与失败处理
- [ ] 写 README

---

## 九、继承自 token-fbi 的已知问题（开发时必须修正）

复刻时不要照抄原代码，以下问题在新项目里直接规避：

1. **XSS 转义不一致**：原项目构建脚本做了 HTML 转义，但浏览器端 render() 没做。新项目里渲染函数统一用一个 `escapeHtml()` 工具函数，模板字符串插入用户可控内容时一律走它
2. **事件绑定丢失**：原项目海报弹窗只在初始化绑一次事件，筛选后 innerHTML 重建导致事件失效。新项目用事件委托（在父容器上监听 click）
3. **错误的缓存头**：原项目 index.html 写了 `no-cache, no-store`，完全放弃缓存。新项目移除这三行 meta，让 CDN 和浏览器正常缓存；静态资源用 `?v=hash` 长缓存
4. **GSAP 过重**：原项目为了几个入场动画加载了 60KB GSAP。新项目用 CSS `@keyframes` + `IntersectionObserver` 实现，零依赖
5. **数据与逻辑耦合**：原项目 30 条卡片数据和渲染逻辑塞在一个 49KB 文件。新项目数据全部走 YAML，JS 只留渲染逻辑
6. **构建脚本脆弱**：原项目靠 `indexOf('function render(type) {')` 字符串切分 app.js。新项目直接读 YAML，不做字符串切分

---

## 十、可移植性说明

项目架构不绑定 Cloudflare。以下组件跨平台时需要调整：

| 组件 | Cloudflare | Vercel | EdgeOne | 自建服务器 |
|---|---|---|---|---|
| 服务端函数入口 | `functions/api/*.js` | `api/*.js` | Edge Function | PHP / Node 任意 |
| 函数签名 | `onRequest(context)` | `(req, res)` | 类似 | 任意 |
| 业务逻辑（调 GitHub / 飞书） | 完全相同 | 完全相同 | 完全相同 | 完全相同 |
| 反垃圾验证码 | Turnstile | Turnstile / hCaptcha | Turnstile | hCaptcha |
| 投稿记录存储 | Cloudflare KV | Vercel KV / Upstash | 腾讯云 COS | 本地 SQLite |

迁移工作量：函数入口外壳约 10 行代码，业务逻辑 0 改动。

---

## 十一、安全与合规

1. **GitHub PAT 最小权限**：只给 Contents: read/write，只限定到这一个仓库，不给全仓库权限
2. **Token 不进前端**：所有 PAT 都在 Pages Function 环境变量里，不暴露到浏览器
3. **Turnstile 必开**：防止机器人往仓库灌垃圾 YAML
4. **外链统一 `rel="noopener noreferrer"`**
5. **管理员账号收敛**：Sveltia CMS 只授权少数协作为 collaborator，不开放公开注册
6. **投稿内容审核前置**：status=pending 的卡片不上线，由管理员人工判断

---

## 十二、待办清单

### 已确认决策（2026-09-19）

- [x] 项目正式名称：**AI CIA**
- [x] 默认域名：**aicia.pages.dev**（后续可绑自有域名）
- [x] 视觉风格：**重新设计，去 AI 味**（编辑刊物风，详见 3.4 节）
- [x] 观望名单模块：**保留**
- [x] 投稿表单字段：**与卡片字段一一对应**（详见 7.3 节），方便管理员审核时直接发布

### 开发任务

- [ ] T1：数据拆分（YAML 化）
- [ ] T2：新构建脚本 build-site.mjs
- [ ] T3：index.html + style.css + app.js（移植并修复 6 个坑）
- [ ] T4：admin/index.html + config.yml
- [ ] T5：OAuth 代理部署
- [ ] T6：contribute.html 投稿表单
- [ ] T7：functions/api/submit.js 投稿接收
- [ ] T8：Turnstile + 飞书机器人接入
- [ ] T9：端到端测试
- [ ] T10：README + favicon + og:image

---

## 十三、关键参考链接

- 参考项目：https://github.com/hope0719/token-fbi
- Sveltia CMS 官方文档：https://sveltiacms.app/en/docs/start
- Sveltia CMS OAuth Worker：https://github.com/sveltia/sveltia-cms-auth
- Cloudflare Pages OAuth 实践：https://sheng.page/en/posts/adding-decap-cms-to-hugo-on-cloudflare-pages/
- Cloudflare Turnstile：https://www.cloudflare.com/products/turnstile/
- Cloudflare Pages 控制台：https://dash.cloudflare.com/
