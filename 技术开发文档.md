# AICIA 技术开发文档

> 版本：v1.1 · 更新日期：2026-09-20
> 本文档是项目从选型到落地的完整技术方案，开发时以此为准。

---

## 一、项目概述

**AI CIA**（项目代号 AICIA）是一个"免费 AI Token 情报站"，汇总各家大模型与 AI 工具的免费额度、支持模型、领取方式和实测评价，以卡片墙形式公开展示。

- 正式名称：**AI CIA**
- 默认域名：https://aicia.pages.dev（后续可绑自有域名）
- 参考项目：https://github.com/hope0719/token-fbi （线上 https://token-fbi.com/）
- 内容形态：卡片墙（大模型类 / 工具类 / 项目类）+ **观望名单（保留）**
- 维护模式：站长与协作者通过可视化后台录入，普通访客通过表单投稿，站长审核后上线
- 成本目标：域名 + Cloudflare 免费额度，月运营成本接近 0

---

## 二、核心设计原则

1. **Git 即数据库**：所有内容（卡片、观望名单、投稿）都是 GitHub 仓库里的 YAML 文件，不用独立数据库
2. **薄后端**：服务端只做三件事——收表单、调 GitHub API、做 OAuth 代理，没有业务逻辑
3. **平台无关**：核心代码不绑定任何 serverless 厂商，未来可迁 Vercel / EdgeOne / 自建 PHP
4. **静态优先**：访客看到的最终页面是构建产物（预渲染 HTML），不需要 JS 执行就能看到内容（SEO 友好）
5. **权限分层**：访客匿名投稿，管理员 GitHub 登录审核，二者身份隔离

---

## 三、技术选型

### 3.1 托管平台

| 项目 | 选择 | 理由 |
|---|---|---|
| 主托管 | **Cloudflare Pages** | 免费、无限带宽、自带 Pages Functions、与 Sveltia CMS 生态最顺 |
| 备用 | Vercel / EdgeOne Pages | 架构已解耦，迁移时只改函数入口文件 |
| 不选 | GitHub Pages | 不支持服务端函数，无法独立做投稿表单与 OAuth |

### 3.2 内容管理后台

**选择 Sveltia CMS**，在 Decap CMS 与 TinaCMS 之间对比后的结论：

| 维度 | Sveltia CMS | TinaCMS |
|---|---|---|
| 后台页面 | 托管在自己域名 | 编辑器核心从 tina.io 拉取 |
| 认证 | 自建 OAuth，同域 | 依赖 Tina Cloud（tina.io） |
| 免费额度 | 完全免费、无人数限制 | 免费版仅 2 用户 |
| 对纯静态 HTML | 加一个 admin/ 目录即可 | 要求 Next.js/Astro 重写 |
| 国内网络 | 不依赖境外 SaaS | 强依赖 tina.io，单点风险 |

Sveltia 是 Decap CMS 的 drop-in 替换，配置格式兼容，CDN 引入：
`https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js`

### 3.3 OAuth 方案（重要：国内网络适配）

**不使用独立的 Cloudflare Workers OAuth 代理**，而是把 OAuth 逻辑直接写成 Pages Function，部署在主站同域。

原因：`*.workers.dev` 域名在国内经常被墙，会导致 CMS 登录跳转超时。把 OAuth 代理放在 `aicia.pages.dev/auth` 和 `/callback` 上，和主站同域，国内访问无问题。

### 3.4 技术栈

- 前端：原生 HTML + CSS + Vanilla JS（不引框架，不引 GSAP，动画用 CSS + IntersectionObserver）
- 构建：Node.js 脚本（`scripts/build-site.mjs`）
- 后端：Cloudflare Pages Functions（原生 JS，无框架）
- 数据格式：YAML（`data/cards/*.yml`）
- 反垃圾：Cloudflare Turnstile
- 通知：飞书自定义机器人 webhook

### 3.5 视觉设计方向：去 AI 味

不复用 token-fbi 的"深色情报局风 + 琥珀金"。目标是做出一份**克制、像传统编辑刊物**的情报小报，而不是又一个 AI SaaS 官网。

**实际落地的视觉风格**：参考作者另一站 ai-pick 的设计语言——
- 米白纸底（#f7f3ea）+ 白色卡片 + teal 主色（#087270）+ 琥珀强调（#d97706）
- 3 列卡片网格（桌面）/ 2 列（平板）/ 1 列（手机）
- 卡片：10px 圆角、细边框、柔和阴影、hover 上浮
- 观望名单：紧凑一行式（序号 + 名称 + 观望标签 + 原因省略 + 查看链接）
- 顶部报头细线 + 吸顶导航 + 毛玻璃模糊

**要避免的"AI 味"特征**：
- 紫蓝渐变、霓虹发光、玻璃拟态大圆角卡片
- 满屏 emoji 装饰、icon 满天飞
- "一站式 / 全栈 / 赋能 / 智能"这类 SaaS 话术
- 模板感强烈的 hero 区

---

## 四、系统架构

### 4.1 三个角色，两套身份

| 角色 | 操作方式 | 是否登录 | 用什么身份 |
|---|---|---|---|
| 访客 | 填表投稿 | 否，匿名 | 无身份，直接 POST |
| Pages Function | 收到表单，写仓库文件 | 对访客透明 | 站长预存的 `GITHUB_PAT`（仓库写权限） |
| 管理员 | 审核、补字段、发布 | 是 | 自己的 GitHub 账号 OAuth |

### 4.2 完整数据流

```
【访客投稿链路】
访客打开 /submit.html
  → 填表（Turnstile 验证）
  → POST /api/submit
  → Pages Function:
      ① 验 Turnstile token
      ② 用 GITHUB_PAT 调 GitHub Contents API
         在 data/cards/ 下新建 yml 文件（status: pending）
      ③ POST 飞书机器人 webhook 通知站长
  → 返回成功页

【管理员审核链路】
管理员打开 /admin/
  → GitHub OAuth 登录（走同域 Pages Function: /auth → /callback）
  → Sveltia CMS 展示"待审核"视图（filter status=pending）
  → 点开投稿，补 rating / 精选判断 / tone
  → 改 status 为 approved
  → Save（commit 到 main）

【上线链路】
main 收到新 commit
  → Cloudflare Pages 自动构建
  → build-site.mjs:
      ① 读 data/cards/*.yml
      ② 只渲染 status=approved 的卡片
      ③ 注入 index.html 占位符
      ④ 输出 dist/（含 robots.txt / sitemap.xml / llms.txt）
  → 静态文件上线
```

### 4.3 架构图

```
                    浏览器
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
   / (卡片站)    /admin/ (CMS)   /submit.html
   静态 HTML     Sveltia SPA     投稿表单
        │             │             │
        │             ↓             ↓
        │        /auth /callback   /api/submit
        │        OAuth Pages Func   投稿 Pages Func
        │             │             │
        │             └──────┬──────┘
        │                    ↓
        │              api.github.com
        │              (读写仓库 YAML)
        ↓
   Cloudflare CDN 静态分发
```

---

## 五、数据模型

### 5.1 卡片字段（data/cards/*.yml）

```yaml
# 平台基本信息
name: "WorkBuddy"                # 必填，平台名
type: "工具"                      # 必填：大模型 / 工具 / 项目
modality: "HY3 · DeepSeek-V4.1"  # 必填，支持的模型

# 评价（管理员补）
rating: 5                        # 必填，1-5 星
tone: "green"                    # 可选：yellow / green / orange / null
badge: "九月限定"                # 可选，右上角角标

# 内容
quota: "免费额度说明..."          # 必填
signup: "注册奖励说明..."         # 可选
effect: "实际体验..."             # 必填
link: "https://..."               # 必填，领取链接

# 状态（投稿时自动填，管理员改）
status: "pending"                 # pending / approved / rejected
updated: "2026-09-19"             # 必填，最近更新日期
limited: "2026-10-10"             # 可选，限时截止日

# 展示控制（管理员配置）
alwaysShow: false                 # 可选，广告卡强制展示
pin: 4                            # 可选，固定位次
```

### 5.2 观望名单（data/watchout.yml）

```yaml
watchout:
  - name: "某平台"
    why: "额度太小 / 不稳定"
    link: "https://..."
```

---

## 六、目录结构

```
AICIA/
├── data/
│   ├── cards/                       # 卡片数据（17 个 yml）
│   └── watchout.yml                 # 观望名单（16 条）
│
├── admin/                           # Sveltia CMS 后台
│   ├── index.html                   # 引用 CDN 脚本
│   └── config.yml                   # collections 与 fields 映射
│
├── functions/                       # Cloudflare Pages Functions
│   ├── auth.js                      # OAuth 第一步：重定向到 GitHub
│   ├── callback.js                  # OAuth 第二步：换 token
│   ├── submit.js                    # 投稿接收端点（T6 待写）
│   └── _lib/
│       └── oauth.js                 # OAuth 共享逻辑
│
├── public/                          # 前端源文件（构建前）
│   ├── index.html                   # 骨架，含占位符
│   ├── style.css                    # 卡片网格风样式
│   ├── app.js                       # 浏览器交互
│   └── images/
│
├── scripts/
│   ├── build-site.mjs               # 构建：读 YAML → 生成 dist/
│   └── validate-cards.mjs           # 数据校验
│
├── package.json
└── .gitignore
```

---

## 七、核心组件设计

### 7.1 Sveltia CMS 配置（admin/config.yml）

```yaml
backend:
  name: github
  repo: xbfuye/aicia
  branch: main
  base_url: https://aicia.pages.dev    # 同域，不需要单独 Worker

media_folder: "public/images"
public_folder: "/images"
publish_mode: editorial_workflow

collections:
  - name: pending        # 待审核投稿（filter status=pending，不可新建）
  - name: approved        # 已上线卡片（filter status=approved，可新建/编辑/删除）
  - name: watchout        # 观望名单（单文件 list widget）
```

环境变量（在 Cloudflare Pages 项目设置中配置）：
- `GITHUB_CLIENT_ID`：GitHub OAuth App 的 Client ID
- `GITHUB_CLIENT_SECRET`：GitHub OAuth App 的 Client Secret（Secret 类型）
- `ALLOWED_DOMAINS`：`aicia.pages.dev`

GitHub OAuth App 的 callback URL 必须设为：`https://aicia.pages.dev/callback`

### 7.2 投稿接收端点（functions/submit.js）

职责：
1. 从 POST body 读取表单字段
2. 调 Cloudflare Turnstile API 验证真人
3. 用 `GITHUB_PAT` 调 GitHub Contents API，在 `data/cards/` 下新建文件
   - 文件名：`平台名-时间戳.yml`，防冲突
   - 默认值：`status: pending`、`rating: 3`、`updated: 今天`
4. POST 飞书机器人 webhook，消息体：新投稿平台名 + 链接 + 摘要
5. 返回 JSON 给前端

环境变量（在 Cloudflare Pages 控制台配置）：
- `GITHUB_PAT`：仓库 Contents 读写权限
- `GITHUB_REPO`：`xbfuye/aicia`
- `TURNSTILE_SECRET_KEY`：Turnstile 密钥
- `FEISHU_WEBHOOK_URL`：飞书群机器人 webhook

### 7.3 投稿表单页（public/submit.html）

表单字段与卡片 YAML 字段**一一对应**，访客填完后，管理员在 CMS 里看到的就是已经填好这些字段的 yml，只需要补专业判断项。

| 表单字段 | 控件 | 必填 | 对应 YAML 字段 |
|---|---|---|---|
| 平台名称 | 单行文本 | 是 | `name` |
| 官网/领取链接 | URL | 是 | `link` |
| 平台类型 | 单选：大模型 / 工具 / 项目 | 是 | `type` |
| 支持哪些模型 | 单行文本 | 是 | `modality` |
| 免费额度说明 | 多行文本 | 是 | `quota` |
| 实际体验/推荐理由 | 多行文本 | 是 | `effect` |
| 是否限时 | 单选 + 日期 | 否 | `limited` |
| 你的联系方式（选填） | 单行文本 | 否 | `contact`（仅管理员可见） |

**管理员在 CMS 里额外补的字段**：rating、tone、badge、alwaysShow、pin、status。

### 7.4 OAuth 代理（Pages Function，同域）

从官方 sveltia-cms-auth Worker 模板移植，改成 Pages Function 格式：
- `/auth`：重定向到 GitHub 授权页，设 CSRF cookie
- `/callback`：接收 GitHub 回调，用 client_id/secret 换 access_token，postMessage 给 CMS 弹窗

**关键决策**：不用独立 Workers，原因是 `*.workers.dev` 域名在国内不稳定，同域 Pages Function 避免了这个问题。

### 7.5 构建脚本（scripts/build-site.mjs）

职责：
1. 读 `data/cards/*.yml`，`js-yaml` 解析
2. 过滤 `status === 'approved'`
3. 应用精选规则（isFeatured）与观望名单排除
4. 排序（pin 字段优先）
5. 把卡片预渲染成 HTML，注入 `index.html` 占位符
6. 复制 `public/` 和 `admin/` 到 `dist/`
7. 生成 JSON-LD、robots.txt、sitemap.xml、llms.txt
8. 输出到 `dist/`

---

## 八、部署步骤

### 阶段 1：本地数据层 ✅

- [x] 把 token-fbi 的 TOKENS 数组拆成 `data/cards/*.yml`（17 张 approved + 16 条观望）
- [x] 写 `scripts/build-site.mjs`，本地 `npm run build` 验证 dist/ 产物
- [x] 写 `scripts/validate-cards.mjs` 数据校验

### 阶段 2：Cloudflare Pages 首次部署 ✅

- [x] 新建 GitHub 仓库 `xbfuye/aicia`，push 代码
- [x] Cloudflare Pages 连接仓库
- [x] 构建命令：`npm run build`，输出目录：`dist`
- [x] 环境变量 `NODE_VERSION=20`
- [x] 已上线：https://aicia.pages.dev

### 阶段 3：CMS 后台 + OAuth ✅

- [x] 写 `admin/index.html` 和 `admin/config.yml`
- [x] 建 GitHub OAuth App（callback: `https://aicia.pages.dev/callback`）
- [x] OAuth 代理写成 Pages Function（`functions/auth.js` + `functions/callback.js` + `functions/_lib/oauth.js`）
- [x] 配置环境变量：`GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` / `ALLOWED_DOMAINS=aicia.pages.dev`
- [x] 验证：打开 `/admin/` 能登录、能看到 3 个 collection

### 阶段 4：投稿链路（T6，进行中）

- [ ] 写 `public/submit.html` 表单
- [ ] 写 `functions/submit.js` 投稿接收端点
- [ ] 建 Cloudflare Turnstile site
- [ ] 建飞书群自定义机器人，拿 webhook URL
- [ ] 配置环境变量：`GITHUB_PAT` / `TURNSTILE_SECRET_KEY` / `FEISHU_WEBHOOK_URL`
- [ ] 端到端测试：匿名提交 → 仓库出现 yml → 飞书收到通知 → CMS 能看到待审核

### 阶段 5：打磨

- [ ] 加 favicon、og:image
- [ ] 写 README
- [ ] 可选：绑自定义域名

---

## 九、继承自 token-fbi 的已知问题（已修正）

1. **XSS 转义**：渲染函数统一走 `escapeHtml()`
2. **事件绑定**：用事件委托，不用单次绑定
3. **缓存头**：移除了 no-cache meta
4. **GSAP 过重**：零依赖，CSS + IntersectionObserver
5. **数据与逻辑耦合**：数据全走 YAML
6. **构建脚本脆弱**：直接读 YAML，不做字符串切分

---

## 十、可移植性说明

| 组件 | Cloudflare Pages | Vercel | EdgeOne |
|---|---|---|---|
| 服务端函数入口 | `functions/*.js` | `api/*.js` | Edge Function |
| 函数签名 | `onRequest(context)` | `(req, res)` | 类似 |
| 业务逻辑（调 GitHub / 飞书） | 完全相同 | 完全相同 | 完全相同 |

迁移工作量：函数入口外壳约 10 行代码，业务逻辑 0 改动。

---

## 十一、安全与合规

1. **GitHub PAT 最小权限**：只给 Contents: read/write，只限定到这一个仓库
2. **Token 不进前端**：所有 PAT 都在 Pages Function 环境变量里
3. **Turnstile 必开**：防止机器人往仓库灌垃圾 YAML
4. **外链统一 `rel="noopener noreferrer"`**
5. **管理员账号收敛**：Sveltia CMS 只授权少数 collaborator
6. **投稿内容审核前置**：status=pending 的卡片不上线

---

## 十二、开发任务进度

### 已确认决策

- [x] 项目正式名称：**AI CIA**
- [x] 默认域名：**aicia.pages.dev**
- [x] 视觉风格：**ai-pick 卡片网格风**（米白纸底 + teal 主色 + 3 列卡片）
- [x] 观望名单模块：**保留**（紧凑一行式）
- [x] 投稿表单字段：**与卡片字段一一对应**
- [x] CMS：**Sveltia CMS**（CDN 加载）
- [x] OAuth：**Pages Function 同域**（不用独立 Worker）

### 任务进度

- [x] T1：数据拆分（17 卡片 + 16 观望）
- [x] T2：新构建脚本 build-site.mjs
- [x] T3：index.html + style.css + app.js（卡片网格风）
- [x] T4：admin/index.html + config.yml
- [x] T5：OAuth 代理（Pages Function 同域）
- [ ] T6：submit.html 投稿表单
- [ ] T7：functions/submit.js 投稿接收
- [ ] T8：Turnstile + 飞书机器人接入
- [ ] T9：端到端测试
- [ ] T10：README + favicon + og:image

---

## 十三、关键参考链接

- 参考项目：https://github.com/hope0719/token-fbi
- 参考设计：https://hope0719.github.io/ai-pick/
- Sveltia CMS 官方文档：https://sveltiacms.app/en/docs/start
- Sveltia CMS OAuth 模板（已移植为 Pages Function）：https://github.com/sveltia/sveltia-cms-auth
- Cloudflare Turnstile：https://www.cloudflare.com/products/turnstile/
- Cloudflare Pages 控制台：https://dash.cloudflare.com/
